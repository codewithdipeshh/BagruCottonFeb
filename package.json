{
  "name": "bagru-cotton-feb",
  "private": true,
  "description": "Monorepo root — run scripts via frontend/ or backend/ workspaces",
  "scripts": {
    "dev": "concurrently \"pnpm dev:customer\" \"pnpm dev:admin\"",
    "dev:customer": "pnpm --filter customer-web dev",
    "dev:admin": "pnpm --filter admin-web dev",
    "build": "pnpm --filter customer-web build && pnpm --filter admin-web build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "@types/node": "^20.0.0"
  }
}
